# ---- XcodeGen 生成物 ----
# .xcodeproj 是派生文件，由 CI 用 xcodegen 生成，不入版本控制，
# 这样才能保证「工程配置的唯一真实来源是 project.yml」。
*.xcodeproj
*.xcworkspace

# ---- 构建产物 ----
build/
DerivedData/
*.ipa
Payload/
verify/

# ---- Xcode 本地状态 ----
xcuserdata/
*.xcuserstate
*.moved-aside
*.hmap

# ---- macOS ----
.DS_Store
._*

# ---- 本地临时目录（模型转换等脚本的中间产物）----
scripts/tmp/
scripts/out/

# ---- 第三方依赖源码与构建产物 ----
# whisper.cpp 与 sherpa-onnx 都由 CI 在构建前克隆并编译。
# 不纳入版本控制：体积大、且必须与 CI 中固定的 tag 保持一致。
third_party/

# ---- 内置模型（CI 构建时下载，随 ipa 打包）----
# sherpa-onnx 的四个模型（VAD / 降噪 / 说话人分割 / 声纹，合计约 35 MB）
# 由 CI 下载到此目录并打进 ipa。不纳入版本控制：二进制文件不适合进 git，
# 且它们与 CI 中固定的 sherpa tag 是一组，应当一起演进。
Resources/SherpaModels/
Resources/WordLists/
